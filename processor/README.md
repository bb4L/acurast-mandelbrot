# acurast-websocket processor

Script for the worker / processor to calculate the mandelbrot heats.

## development

Can be run with `npx tsx dev.ts` it will output the address for the created processor.
